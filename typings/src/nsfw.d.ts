export = NSFW;
/**
 * @typedef {object} NSFW
 * @see {NSFW}
 * @example const nsfw = new NSFW()
 * @type {Class}
 */
declare class NSFW {

  /**
 * https://discord.gg/luppux
 * @description It allows you to capture the specified NSFW category via the API, and the "image" object structure is returned via the API.
 * @param {string} type NSFW Category.
 * @returns {object}
 * @example await NSFW.fetch("ass")
 * @async
 */
  fetch(type:'ass'|'sixtynine'|'pussy'|'dick'|'anal'|'boobs'|'ass'|'bdsm'|'black'|'easter'|'bottomless'|'blowjub'|'collared'|'cum'|'cumsluts'|'dp'|'dom'|'extreme'|'feet'|'finger'|'fuck'|'futa'|'gay'|'gif'|'group'|'hentai'|'kiss'|'lesbian'|'lick'|'pegged'|'phgif'|'puffies'|'real'|'suck'|'tattoo'|'tiny'|'toys'|'xmas'):{status:string,image:{type:string,url:string}};

}

//# sourceMappingURL=level-up.d.ts.map
